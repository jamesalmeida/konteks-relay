# 2026-02-02 — Massive Build Day

## OpenClaw Mission Control
- Built the entire Mission Control dashboard from scratch (Next.js, Tailwind, dark space theme)
- Repo: github.com/jamesalmeida/openclaw-mission-control
- Deployed to Vercel at openclaw-mission-control.vercel.app
- Uses same Supabase DB as Konteks Web (shared folders/items tables)
- Added `project_metadata` table to Supabase for MC-specific data (github_repo, kanban_status, color, notes)

### Features built (all via Codex):
- **v2 rebuild**: Supabase auth, live project sync, GitHub API integration
- **Issue #3**: Task-based kanban (tasks on board, not projects) + project detail pages + project filter
- **Issue #1**: 🐙 octopus favicon
- **Issue #2**: Hide projects from MC (three-dot menu, HIDDEN_FROM_MC marker in notes)
- **Issue #5**: Fixed GitHub PR/issue counts (catch-all route [...repo])
- **Issue #7**: Task detail modal + three-dot menu with delete
- **Issue #11**: Activity tab with task events + GitHub events timeline
- **Issue #12**: Created for future agent activity (blocked by gateway API)
- **Issue #13**: Dark/light/system mode toggle (had to fix text-white → theme-aware text)
- **Issue #15**: Low priority option (syncs with Konteks "someday")
- **Issue #16**: Slide-up/down modal transitions
- **Issue #19**: Drafted task assignment system (assign to James/Tersono, auto-pickup on heartbeat)

### Architecture:
- Projects = Konteks folders (folder_type='project')
- Tasks = Konteks items (linked via folder_id)
- Kanban status stored in items.smart_list field
- project_metadata table: github_repo, kanban_status, color, notes, hidden flag
- GitHub API: /api/github/[...repo] route, /api/github/events route
- Agent status: /api/agent/status (placeholder, ready for gateway API)

## Konteks Web Updates
- Fixed dark mode hydration error (suppressHydrationWarning on <html>)
- Renamed env vars: ANON_KEY → PUBLISHABLE_KEY, SERVICE_ROLE_KEY → SECRET_KEY
- Accidentally committed .env with Supabase creds — removed immediately, updated .gitignore
- Issue #67: Rename Someday → Low priority (Codex working)
- Issue #68: Remove Tersono/OpenClaw UI elements, make clean standalone todo app (Codex working)

## Supabase Key Rotation
- Migrated from legacy JWT secret to new JWT Signing Keys + API Keys system
- Rotated keys — old leaked keys are now invalid
- New key format: sb_publishable_* (anon) and sb_secret_* (service_role)
- Updated env var names in both repos to match new naming convention
- Updated Vercel env vars for both projects

## Schema: project_metadata table
```sql
create table project_metadata (
  id uuid primary key default gen_random_uuid(),
  folder_id uuid references folders(id) on delete cascade,
  user_id uuid references auth.users(id),
  github_repo text,
  kanban_status text default 'backlog',
  color text,
  notes text,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  unique(folder_id)
);
```

## Reminder Set
- Tomorrow 7:00 AM: Pack underwear and pants for Ruby to bring to school

## Git Workflow Lesson
- exec `cd` commands don't persist between calls — always use `workdir` parameter
- Force push from wrong directory pushed workspace files to MC repo (fixed)
- Use git worktrees for parallel Codex work — much cleaner than branching in main repo

## Key Decision: Separation of Concerns
- Konteks Web = consumer todo app (clean, no AI branding)
- Mission Control = internal ops dashboard (projects, kanban, agent status, GitHub)
- Both share same Supabase DB
- MC adds metadata layer on top of Konteks data

## Evening Session — Skills & Tools

### Solar Tax Credit Research
- Section 25D (residential) expired Dec 31, 2025 — no more personal solar credit
- Section 48 (commercial ITC) still alive but phasing out — must begin construction before July 4, 2026
- S-Corp could theoretically claim commercial ITC on business-use portion of home solar, but IRS would scrutinize
- Better path: lease/PPA where solar company claims credit and passes savings as lower rates

### Tax Professional Skill
- Installed from ClawHub earlier in the day — comprehensive US tax advisor skill

### Skills Audit & Installation Spree
- Installed from ClawHub: **pm-odds** (Polymarket), **yahoo-finance**, **crypto-wallet**, **advanced-skill-creator**
- yahoo-finance needed `uv` to run (PEP 723 inline deps) — works via `uv run --with yfinance`
- crypto-wallet = read-only portfolio tracker for James's wallets, can also send with approval
- advanced-skill-creator = meta-skill for building other skills using 5-step research flow

### Missing Skills Inventory
- 24 skills missing CLI deps — documented install commands for James
- Key ones worth installing: clawhub (done), ripgrep, tmux, himalaya
- Niche hardware ones: bear-notes, blucli, camsnap, eightctl, openhue, sonoscli, spotify-player
- Env var needed: GOOGLE_PLACES_API_KEY (goplaces + local-places), NOTION_API_KEY, TRELLO_API_KEY

### Spaces Listener Audit (7/10)
- Audited James's spaces-listener skill using advanced-skill-creator methodology
- **Issues found:**
  1. Missing `metadata.openclaw.requires` — OpenClaw can't detect deps
  2. No `when` or `examples` in frontmatter — agents won't auto-trigger
  3. Version mismatch — SKILL.md says 1.4.0, script says 1.3.0
  4. Script not auto-added to PATH
  5. Summarize hardcodes gpt-4o-mini
  6. No cleanup of stale PID/meta files
  7. ClawHub version way behind (v0.1.2 vs v1.4.0 locally)
- **Suggestions:** API whisper fallback for large files, `spaces replay` alias, notification on completion
- James may want me to fix these and push updated version

### Quick Lookups
- TSLA: $421.81 (-2.00%) as of market close Feb 2
- Polymarket trending: govt shutdown (99.9% yes, $34.8M), Kevin Warsh Fed chair nom (98.2% yes)
