# 2026-02-12 — Big Actually Useful AI Dev Day

## Issues Closed (11 total!)
- **#38** — Verified 1-day free trial works correctly on Stripe
- **#8** — Dashboard shows 'running' status + account email after provision
- **#40** — Fixed Railway domain collision (service names now unique per user: `gateway-${tier}-${userId.slice(0,8)}`)
- **#41** — Dashboard shows cancellation status when sub is pending cancel (`cancelAtPeriodEnd` field added to Convex schema)
- **#42** — Tier upgrade now uses `stripe.subscriptions.update()` instead of creating new subscription (was double-billing)
- **#9** — Gateway token + "Open Your AI Assistant" button on dashboard
- **#34** — Admin page gets distinct title "⚙️ Admin" + gear favicon
- **#44** — "Powered by OpenClaw" badge replaces old hero text
- **#11** — Cleaned up stale test subscriptions in Convex + Railway
- **#43** — Delete Account flow (Stripe cancel + Railway deprovision + Convex auth cleanup)
- **#45** — Email/password auth normalized to lowercase to prevent duplicate accounts

## Other Fixes (no issue)
- **PORT conflict fix** — Railway containers were crashing because PORT=18789 conflicted with gateway. Changed to PORT=8080 for proxy.
- **setSaleSettings admin auth** — Removed Convex auth guard since admin page uses client-side password (issue #29 pattern)
- **Google OAuth email storage** — Added `createOrUpdateUser` callback to save email+name from Google profile to users table
- **Convex deploy reminder** — Multiple times forgot to deploy Convex functions after merging. ALWAYS deploy Convex after schema/function changes.

## IAS Consulting Meeting (9am)
- SOW review with Cam (Oracle/NetSuite), Abdo, Jason
- **SOW approved** — DocuSign going out, Abdo signing Monday noon
- **$113K year 1** ($58K implementation + $55K licensing), **$55K/year after** (3% max increase)
- 6-month payment deferral selected (starts Sept 1st, no interest)
- AFC subsidiary handled in Section D (one parent, two subsidiaries)
- Bartender stays for labeling, Ship Central covers pallet manifest
- Claude AI integration included in implementation
- Full meeting notes saved to: `Obsidian-Vault/1 - Projects/IAS Consulting/Phase 1/2026-02-12 Meeting with Cam to go over SOW.md`

## Google OAuth Tokens
- All 3 gog accounts re-authed (tokens expire every 7 days due to unverified app)
- Issue #39 created for Google OAuth verification needed for customer-facing use

## Architecture Notes
- **Actually Useful AI flow:** Sign up → Stripe checkout (with trial) → Webhook → Railway auto-provision → Dashboard shows token → One-click open
- **Railway provisioning:** Each user gets unique project (`fastclaw-${userId.slice(0,8)}`) and service (`gateway-${tier}-${userId.slice(0,8)}`)
- **Convex deployment caveat:** Merging to main deploys Vercel but NOT Convex. Must run `CONVEX_DEPLOYMENT=ideal-ferret-88 npx convex deploy --yes` separately.
- **Admin page auth issue (#29):** Admin uses client-side password `planets-mgm-TEMPER`, but some Convex mutations check `getIsAdmin()` which requires Convex Auth. Remove auth guards from admin mutations or fix #29 properly.

## Remaining Open Issues
- #29 — Secure admin with Convex auth
- #20 — Setup support@actually-useful-ai.com
- #19 — Google OAuth integration for customer gog
- #39 — Google OAuth verification + multi-account
- #13 — Model logos + channel logos
- #12 — Scarcity meter
- #10 — Phase 6: Channel onboarding
- #7 — Phase 5 (mostly done, can close)
- #6 — Social proof toast style fixes
- #5 — Social proof toast: fake → real

## Moltbook API Issue
- API key `moltbook_sk_2VUD7G_AfpK9uhXqwN8DQkhHSjCNyvjJ` gets "No API key provided" error despite being valid
- Redirect from http to https may be stripping the Authorization header
- Need to investigate further

## James's Preferences (reinforced)
- Wants Codex for coding tasks, not me editing directly
- Rapid prototyping mode: work directly on main for Actually Useful AI
- Acknowledges tasks immediately before starting work
