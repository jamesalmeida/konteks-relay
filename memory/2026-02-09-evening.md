# 2026-02-09 Evening — FastClaw → Actually Useful AI

## Major Accomplishments
- Converted fastclaw-web from static HTML to **Next.js 15** (App Router, TypeScript, Tailwind)
- Added **dark/light/system theme toggle** (next-themes)
- Wired **waitlist to Convex** (prod: `ideal-ferret-88`)
- Added **Convex Auth** (Google OAuth + email/password) — both working
- Created **sign in, sign up, dashboard** pages
- **Rebranded to "Actually Useful AI"** — bought actually-useful-ai.com
- Both domains live: actually-useful-ai.com + fastclaw.app → same Vercel deploy
- Added **CLAUDE.md + docs/** (roadmap, architecture, pricing)
- Lobster mascot in footer

## Final Pricing (v1)
- **BYOK** $9/mo — bring your own API key (Anthropic/OpenAI/Grok), any model
- **Pro** $29/mo — AI included (Kimi K2), zero setup, featured tier
- **Premium** $59/mo — choose top-tier models (Claude/GPT/Grok), soft cap → falls back to K2 (Coming Soon)

## Key Decisions
- **Kimi K2 as default model** — cheap, capable, great margins
- **No channel differentiation** between tiers — most people just use one
- **BYOK is the cheap techie tier**, Pro is the normie tier
- **Premium deferred** — coming soon, model choice + soft cap
- **Convex Auth** over Clerk/NextAuth — keeps everything in one place
- **Same Convex project** will eventually be shared with iOS relay app
- **actually-useful-ai.com** is the customer-facing brand, FastClaw is the platform/infra name

## Rebrand
- **FastClaw → Actually Useful AI** (customer-facing brand)
- **Domain:** actually-useful-ai.com (primary) + fastclaw.app (still works)
- **Repo renamed:** `jamesalmeida/actually-useful-ai` (was fastclaw-web)
- **Local folder:** `~/dev/actually-useful-ai`

## Infrastructure
- **Convex project:** fastclaw-web (dev: `elated-gecko-980`, prod: `ideal-ferret-88`)
- **Vercel:** Arkham Ventures team, fastclaw-web project
- **Domains:** actually-useful-ai.com + fastclaw.app (both Cloudflare → Vercel)
- **Google OAuth:** configured in Google Cloud Console, credentials in Convex env vars
- **JWT keys:** generated and set (JWT_PRIVATE_KEY + JWKS) on both dev and prod

## Auth Debugging Notes
- Codex's `npx @convex-dev/auth` init didn't properly set JWT_PRIVATE_KEY and JWKS
- Had to manually generate RSA keypair and set both env vars
- auth.config.ts was wrong (manual OAuth config) — simplified to just domain/applicationID
- Google OAuth env vars needed both `GOOGLE_CLIENT_ID` AND `AUTH_GOOGLE_ID` naming

## Next Steps
1. Stripe checkout integration
2. Docker image (fastclaw-gateway repo)
3. Railway provisioning
4. Channel onboarding in dashboard
