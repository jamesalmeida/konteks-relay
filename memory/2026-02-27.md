# 2026-02-27

## Nightly Build (2:00 AM)
- Revenue Tracker dashboard — standalone HTML, Dropbox demo
- Dark mode, editable data config, bar chart + goal ring + projections
- Codex CLI still broken: 401 auth errors on `/v1/responses` endpoint. Same issue as Feb 26. Needs investigation — possibly `codex login`, API key scope issue, or CLI version update from 0.104.0.
- FastClaw issue #16 already merged (not closed on GH). #13 (file viewer) needs Convex backend access — skipped.
