# 2026-02-24

## Nightly Build (2:05 AM)
- PR #24 on FastClaw: Multi-instance support (connect to multiple OpenClaw instances from one app)
- Instance picker in sidebar, management in Settings, auto-migration, name prompt on pairing
- 3 new files + 4 updated
- **Codex CLI issue:** auth.json is set to ChatGPT auth mode instead of API key — all models fail. Fix: update `~/.codex/auth.json` to set `auth_mode` to `"api-key"`
- Built directly (not via Codex) due to the auth issue

## Queued for morning
- Deliver nightly build update to James
- Flag Codex auth issue
