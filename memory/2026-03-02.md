# 2026-03-02

## Nightly Build (2:00 AM)
- **Sheldn.ai admin panel security fix** — PR #112 on `jamesalmeida/sheldn`
- Replaced hardcoded client-side password with Convex auth + ADMIN_EMAIL_ALLOWLIST
- Created shared `convex/lib/admin.ts` with `getIsAdmin()` / `requireAdmin()` helpers
- Added `requireAdmin()` to ALL admin mutations in settings.ts (10 mutations) and testimonials.ts (4 mutations)
- Frontend now uses `useConvexAuth()` + `getAdminWidgetState` query for 3-tier auth gate
- Closes GitHub issue #29 — was marked "must fix before public launch"
- Branch: `fix/secure-admin-panel`
- ⚠️ After merge: must run `CONVEX_DEPLOYMENT=ideal-ferret-88 npx convex deploy --yes`
- Codex CLI 0.104.0 working fine

## Mercury Rx — App Store Submission! 🚀
- **Submitted to App Store review** — Waiting for Review as of ~10am PST
- Build version: 1.0 (2026.02.26.5), iPhone + iPad
- Bundle ID: `com.mercuryrx.app`, Widget: `com.mercuryrx.app.widget`
- IAP: Widget Pack (`com.mercuryrx.widgets`) — non-consumable, $1.99
- Privacy: `https://jamesalmeida.me/mercury-rx/privacy`
- Support: `https://jamesalmeida.me/mercury-rx/support`
- Both pages added to jamesalmeida-me repo (PR #6, merged)
- GitHub issue #10 created — build dedicated landing page + mercuryrx.app domain later ($14.50 available)

### Changes made during session:
- Merged PR #9 (retrograde notifications) to main
- Added test notification button (debug only, 5-second delay)
- Fixed button contrast in retrograde dark mode (cyan bg + black text)
- Tinted all toggles cyan for dark mode visibility
- Removed "Apple Watch (coming soon)" copy (App Store compliance)
- Added debug toggle to force unpurchased state (for paywall screenshots)
- Added Privacy Policy + Support links in Settings ("LEGAL" section)
- Added `ITSAppUsesNonExemptEncryption = NO` to widget extension
- Synced widget CFBundleVersion with main app
- Multiple build bumps: .1 → .2 → .3 → .4 → .5

### Codex CLI auth broken
- OpenAI API returning 401 Unauthorized — Codex couldn't run
- Made all code changes directly instead
- Need to investigate OPENAI_API_KEY

## Ongoing from prior sessions (late Feb)

### Exec Approvals Fix (Feb 25)
- **RESOLVED**: Added `"ask": "off"` to `tools.exec` in `openclaw.json` — this was the missing piece
- Also set `security: "full"` + `ask: "off"` + `askFallback: "full"` in `exec-approvals.json`
- Both were needed — the effective policy is the stricter of `tools.exec.*` and approvals defaults

### LaunchAgent Env Vars (Feb 25)
- Added all `.zshrc` tokens to `~/Library/LaunchAgents/ai.openclaw.gateway.plist`
- Includes: OP_SERVICE_ACCOUNT_TOKEN, THINGS_AUTH_TOKEN, XAI_API_KEY, GOG_ACCOUNT, HA_TOKEN, X_API_KEY, X_API_SECRET, X_BEARER_TOKEN, KONTEKS_API_KEY
- Documented as security debt in SECURITY.md — should use `op run` wrapper instead eventually

### Discord Channels Added (Feb 19-26)
- **Areas** category: #wagner-st, #health, #finances, #arkham-ventures (moved)
- **🎮 Gaming** category: #arc-raiders
- **🧠 Research**: #politics
- **💼 Projects**: #mercury-rx
- All added to OpenClaw discord config

### OpenClaw Updates
- Updated through v2026.2.22 (Discord voice chat, stream preview, Mistral, auto-updater)
- Codex CLI not installed on Mac mini — needs reinstall

### Lauren's Reminders → Asana
- Lauren wants to move iOS Reminders lists to Asana
- Plan: She shares lists with James → remindctl reads them → generate CSV for Asana import
- Status: Waiting for her to share the lists

### Network Upgrade
- New router/mesh installed at Wagner St
- Mac mini IP: 192.168.184.133
- Wagner Mac mini may have fried HDMI port (running headless is fine)

## New Operating Rule (9:22 PM)
- **"No proof = didn't happen"** — Never say "done" or "working on it" unless the action has actually started. Every status update must include proof (process ID, file path, URL, or command output). A false completion is worse than a delayed honest answer. James explicitly requested this.
