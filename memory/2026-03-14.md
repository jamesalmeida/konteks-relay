# 2026-03-14 — Saturday

## Nightly Build: Admin Delete User Cascade Fix (2:00 AM)
- Fixed **broken admin delete user flow** on Sheldn
- PR #151: https://github.com/jamesalmeida/sheldn/pull/151
- Branch: `fix/admin-delete-cascade`
- **3 bugs fixed:**
  1. Delete button was hardcoded `disabled` on user detail page — now functional with 2-step confirmation
  2. Admin delete didn't deprovision Railway — orphaned projects costing money — now calls `deprovisionInstance()`
  3. Admin delete didn't cancel Stripe subscriptions — leaked billing — now calls `stripe.subscriptions.cancel()`
- Created new `/api/admin/user/delete` API route for full cascade
- Added `systemDeleteUser` Convex mutation (no auth context needed for server routes)
- Updated both user detail and users list pages to use cascade API
- TypeScript compiles clean
- Picked because James found these bugs yesterday during admin testing, frustration was HIGH, and orphaned resources cost real money
