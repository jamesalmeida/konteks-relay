# 2026-02-13 — AUAI Google Workspace Integration Day

## Major Accomplishments
- **Google Workspace OAuth flow working end-to-end**: User clicks Connect → Google consent → callback exchanges tokens → stores in Convex → pushes env vars to Railway → triggers redeploy → gog auto-configured
- **Multi-account Google support**: Users can connect multiple Google accounts (personal + work). Each shows in dashboard with individual disconnect buttons.
- **Docker entrypoint auto-configures gog**: Reads GOG_ACCOUNTS_JSON (base64 array), imports each account into gog keyring. Falls back to single GOG_GOOGLE_* vars.
- **AGENTS.md and SOUL.md auto-updated on restart**: Entrypoint always refreshes managed files from Docker image.
- **SOUL.md teaches agent about hosted context**: Knows it's managed service for non-technical users, never mentions Docker/SSH/terminals.
- **Dashboard UX polish**: Instance card redesigned (big launch button, technical details behind chevron), mobile layout fixed (stripped card styling, full width).
- **Railway project naming**: New instances get human-readable names (`james-almeida-premium-k17fpneq`), stable service domains (`auai-k17fpneq`).

## Issues Created
- #63: Auto-redeploy customer instances on new Docker image (staged rollout)
- #65: Multi-account Google Workspace support ✅ merged
- #67: Rename Railway projects/services with user names ✅ merged

## Key Fixes
- OAuth callback: removed broken convexAuthNextjsToken, uses HMAC-signed state
- Token import format: matches gog's export format (email, services, scopes, refresh_token)
- Calendar scopes: added calendar.readonly + calendar.events
- Schema: email field optional for backward compat
- Disconnect: uses tokenId instead of email (works for old tokens)
- Mobile: stripped dashboard-card padding/border/bg on mobile, reduced top padding

## Codex Issues
- Codex failed to commit twice in actually-useful-ai repo (explored but didn't commit)
- Faster to make changes manually for this repo
- Codex still useful for greenfield/new repos

## Docker Image Commits (fastclaw-gateway)
- entrypoint: gog auto-configure from env vars
- entrypoint: correct gog token export format
- entrypoint: multi-account GOG_ACCOUNTS_JSON support
- entrypoint: always update AGENTS.md and SOUL.md on startup
- workspace/AGENTS.md: gog instructions, calendar, timezone
- workspace/SOUL.md: hosted context for non-technical users
- TZ env var set on Premium instance (America/Los_Angeles)

## Obsidian Templates
- Saved AGENTS.md and SOUL.md templates to `~/Obsidian-Vault/1 - Projects/actually-useful-ai.com/templates/`

## Test Accounts
- Premium: gateway-premium-k17fpneq (jimmyplaysdrums@gmail.com) - 3 Google accounts connected
- Pro: gateway-pro-k174cp72 (youcanalwaysreachjames@gmail.com)
